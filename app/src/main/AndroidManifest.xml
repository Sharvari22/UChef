<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uchef">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UChef">
        <activity android:name=".MoreAboutChef"></activity>
        <activity android:name=".Verification" />
        <activity android:name=".DishDetailActivity" />
        <activity android:name=".Dishes" />
        <activity android:name=".ImagesActivity" />
        <activity android:name=".FoodGallery" />
        <activity android:name=".AddDishes" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".SearchActivity" />
        <activity
            android:name=".ChefMainPage"
            android:label="@string/title_activity_chef_main_page"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.UChef.NoActionBar" />
        <activity android:name=".ChefLoginActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>